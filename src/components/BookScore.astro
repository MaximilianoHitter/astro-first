---
import { SCORE_API_ENDPOINT } from "astro:env/server";
const res = await fetch(SCORE_API_ENDPOINT);
let scoreValue = await res.json();
---

<div class="flex justify-center">
  <span class="" id="score">🖖 Puntuación: {scoreValue.data}/5</span>
</div>
